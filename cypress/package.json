{
  "scripts": {
    "config": "node ./main/utils/data/filesParser.js",
    "config:parallel": "node ./main/utils/data/filesParser.js --parallel",
    "load:parallel": "npm run config:parallel && cypress run --headed --browser chrome",
    "start:ci": "docker run -d -p 80:80 --name amanat24_front $CI_REGISTRY_IMAGE/$CI_COMMIT_REF_NAME:$CI_COMMIT_SHORT_SHA & npx wait-on http://172.17.0.2",
    "test": "npm run config && cypress run --headed --browser chrome",
    "test:split": "npm run config && cypress run --browser chrome --env split=true",
    "test:parallel": "cypress-parallel -s load:parallel -w './artifacts/parallel-weights.json' -t 2 -d './tests/specs/*Suite.js' -b"
  },
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.40.0",
    "@testing-library/cypress": "^9.0.0",
    "allure-commandline": "^2.23.1",
    "axios": "^0.21.1",
    "cypress": "12.14.0",
    "cypress-localstorage-commands": "^2.2.4",
    "cypress-log-filter": "^1.0.4",
    "cypress-parallel": "^0.14.0",
    "cypress-plugin-multiple-click": "^1.0.0",
    "cypress-split": "^1.18.5",
    "cypress-xpath": "^2.0.1",
    "dotenv": "^16.3.1",
    "moment": "^2.29.4",
    "mysql2": "^3.4.1",
    "qs": "^6.11.2",
    "wait-on": "^7.2.0",
    "xml2js": "^0.6.2"
  }
}